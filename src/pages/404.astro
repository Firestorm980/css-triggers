---
import Layout from '@layouts/Layout.astro';

const title = "CSS Triggers";
const base = import.meta.env.BASE_URL
---

<Layout title={title}>
	<h1 class="screen-reader-text">{title}</h1>
	<p>Uh... what? <a href={base}>Go home</a>.</p>
</Layout>
